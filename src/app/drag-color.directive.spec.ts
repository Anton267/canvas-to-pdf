import { DragColorDirective } from './drag-color.directive';

describe('DragColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DragColorDirective();
    expect(directive).toBeTruthy();
  });
});
